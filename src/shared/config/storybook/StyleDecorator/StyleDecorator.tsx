import { Story as StoryType } from "@storybook/react";
// eslint-disable-next-line fsd-path-checker-by-dandoniev/fsd-layer-imports
import "@/app/styles/index.scss";

export const StyleDecorator = (Story: StoryType) => <Story />;
